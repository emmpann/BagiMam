<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BagiMam" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="fontFamily">@font/mulish</item>
        <item name="android:statusBarColor">#80000000</item> <!-- Warna abu-abu transparan -->
        <!-- Menggunakan colorBackground -->

    </style>

    <style name="Theme.BagiMam" parent="Base.Theme.BagiMam" />

    <style name="ProfileRounded">
        <item name="android:layout_height">580dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">@drawable/profile_shape</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:padding">30dp</item>
    </style>

    <style name="OnBoardingDescBG">
        <item name="android:layout_height">435dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">@drawable/onboarding_desc_bg</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:padding">30dp</item>
    </style>
</resources>
