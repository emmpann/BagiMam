<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BagiMam" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BagiMam" parent="Base.Theme.BagiMam" />

    <style name="OnBoardingDescBG">
        <item name="android:layout_height">435dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">@drawable/onboarding_desc_bg</item>
        <item name="android:layout_alignParentBottom">true</item>
        <item name="android:padding">30dp</item>
    </style>
</resources>